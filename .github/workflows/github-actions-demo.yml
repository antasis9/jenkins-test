name: Github Actions Demo
on: [push]
jobs:
  coverage:
      permissions:
        checks: write
        pull-requests: write
      runs-on: self-hosted
      steps:
          - uses: actions/checkout@v3
          - uses: ArtiomTr/jest-coverage-report-action@v2
